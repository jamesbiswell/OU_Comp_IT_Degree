AWSTemplateFormatVersion: '2010-09-09'
Description: Create a demo S3 bucket
Resources:
  DemoBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "cf-demo-bucket-${AWS::AccountId}-${AWS::Region}"
Outputs:
  BucketName:
    Value: !Ref DemoBucket